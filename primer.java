/*public class Primer {
    // Объявления переменных экземпляров
    public void measurementsChanged() {
        float temp = getTemperature();
        float humidity = getHumidity(); Чтобы получить обновленные данные, мы вызываем (уже реализованные) get-методы класса WeatherData
        float pressure = getPressure();
        currentConditionsDisplay.update(temp, humidity, pressure);
        statisticsDisplay.update(temp, humidity, pressure);
        forecastDisplay.update(temp, humidity, pressure); //приходится вносить изменения
    }
    // Другие методы WeatherData
}*/